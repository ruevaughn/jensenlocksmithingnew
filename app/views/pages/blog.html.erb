<% @blog_posts.each do |blog_post| %>
<div class="jumbotron">
  <h1><%= blog_post.header %></h1>
  <h3><%= blog_post.created_at.strftime("%B %-d, %Y") %></h3>
  <p><%= simple_format(blog_post.body) %></p>
</div>
<% end %>
